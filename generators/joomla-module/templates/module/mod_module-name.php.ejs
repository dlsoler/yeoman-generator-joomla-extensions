<?php
/**
 * @package     <%= namespace %>
 * @subpackage  <%= moduleName %>
 *
 * @copyright   <%= copyright %>
 * @license     <%= license %>
 */

defined('_JEXEC') or die;

if ($params->def('prepare_content', 1))
{
	JPluginHelper::importPlugin('content');
	$module->content = JHtml::_('content.prepare', $module->content, '', 'mod_<%= moduleName %>.content');
}

$moduleclass_sfx = htmlspecialchars($params->get('moduleclass_sfx'), ENT_COMPAT, 'UTF-8');

require JModuleHelper::getLayoutPath('mod_<%= moduleName %>', $params->get('layout', 'default'));
